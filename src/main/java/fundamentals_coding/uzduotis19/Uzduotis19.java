package fundamentals_coding.uzduotis19;

public class Uzduotis19 {
    //Write an application that consists of few classes:
    //a. Author class, representing an author – poem writer,
    // which consists of fields surname and nationality (both of type String)
    //b. Poem class, representing poem, which consists of fields
    // creator (type Author) and stropheNumbers (type int – numbers of strophes in poem)
    //c. Main class, with main method, inside which you will:
    //i. Create three instances of Poem class, fill them with data
    // (using constructor and/or setters) and store them in array
    //ii. Write the surname of an author that wrote the longest poem
    // (let your application calculate it!)
}
